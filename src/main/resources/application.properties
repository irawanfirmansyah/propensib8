# # Konfigurasi untuk koneksi MySql
# spring.datasource.platform=mysql
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# # Sesuaikan NAMA_DATABASE dengan nama database anda
# spring.datasource.url=jdbc:b85afa64e49efe:9a7c033f@us-cdbr-iron-east-02.cleardb.net/heroku_b03937c87aa1cca?reconnect=true

# # Sesuaikan dengan USERNAME dan PASSWORD mysql anda
# spring.datasource.username=b85afa64e49efe
# spring.datasource.password=9a7c033f

# # Optimize query untuk db MySql
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5InnoDBDialect

# # Pembuatan database (create || create-drop || validate || update)
# spring.jpa.hibernate.ddl-auto=update

# ## Hibernate Logging
# logging.level.org.hibernate.SQL= DEBUG

# # Initialize the datasource with available DDL and DML scripts
# spring.datasource.initialization-mode=always

# ## Jackson Properties
# spring.jackson.serialization.WRITE_DATES_AS_TIMESTAMPS= false
# spring.jackson.time-zone= UTC

# ## App Properties
# app.jwtSecret= JWTSuperSecretKey
# app.jwtExpirationInMs = 604800000

# #Server Properties
# server.port=2016

# Konfigurasi untuk koneksi MySql
spring.datasource.platform=mysql
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Sesuaikan NAMA_DATABASE dengan nama database anda
spring.datasource.url=jdbc:mysql://localhost:3306/propensi-b8?useSSL=false&serverTimezone=Asia/Jakarta

# Sesuaikan dengan USERNAME dan PASSWORD mysql anda
spring.datasource.username=root
spring.datasource.password=

# Optimize query untuk db MySql
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5InnoDBDialect

# Pembuatan database (create || create-drop || validate || update)
spring.jpa.hibernate.ddl-auto=update

## Hibernate Logging
logging.level.org.hibernate.SQL= DEBUG

# Initialize the datasource with available DDL and DML scripts
spring.datasource.initialization-mode=always

## Jackson Properties
spring.jackson.serialization.WRITE_DATES_AS_TIMESTAMPS= false
spring.jackson.time-zone= UTC

## App Properties
app.jwtSecret= JWTSuperSecretKey
app.jwtExpirationInMs = 604800000

#Server Properties
server.port=2016